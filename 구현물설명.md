# Mini Redis 구현물 설명

이 문서는 평가자가 기능을 직접 확인하고, 구현자가 각 설계 이유를 코드에 근거해 설명하기 위한 순서입니다. 단순히 답을 외우기보다 아래의 “보여줄 코드 → 실행할 명령 → 설명할 말” 순서로 진행합니다.

## 프로젝트 소개

> 이 프로젝트는 Python 내장 `dict`, `set`, `collections`, `heapq` 없이 이중 연결 리스트, 체이닝 해시맵, 최소 힙을 직접 구현한 CLI 기반 Mini Redis입니다. 실제 값은 해시맵에 저장하고, LRU는 해시맵과 이중 연결 리스트를 결합하며, TTL은 최소 힙과 TTL 해시맵의 lazy deletion으로 관리합니다. 메모리는 과제 공식인 키와 값의 UTF-8 바이트 합만 계산합니다.

먼저 다음 명령으로 전체 테스트를 보여줍니다.

```bash
python3 -m unittest -v
```

## 항목 1: 기능 완성도 시연

### 1-1. String 기본 명령 6개

보여줄 파일:

- `mini_redis.py`: `set`, `get`, `delete`, `exists`, `dbsize`, `keys`, `execute`
- `test_mini_redis.py`: `StringCommandTests`

CLI 시연:

```text
SET name "Alice Kim"
GET name
EXISTS name
DBSIZE
KEYS
DEL name
GET name
```

설명:

- `SET`은 저장과 LRU 갱신 후 `OK`를 반환합니다.
- `GET`은 만료 여부를 먼저 처리하며 성공한 조회만 LRU를 갱신합니다.
- `DEL`은 `_delete_key`를 통해 데이터·메모리·LRU·TTL을 함께 정리합니다.
- `DBSIZE`는 해시맵이 유지하는 개수를 반환하고, `KEYS`는 전체 항목을 순회하므로 각각 핵심 조회 비용이 `O(1)`, `O(n)`입니다. 두 명령 전에는 만료된 키를 먼저 정리합니다.

### 1-2. LRU 자동 제거

보여줄 파일과 메서드:

- `mini_redis.py`: `_touch`, `_evict_if_needed`, `_delete_key`, `set`, `get`
- `test_mini_redis.py`: `test_successful_get_changes_lru_eviction_target`

CLI 시연:

```text
CONFIG SET maxmemory 4
SET a 1
SET b 2
GET a
SET c 3
GET a
GET b
GET c
INFO memory
```

예상 핵심 결과:

```text
GET b → (nil)
used_memory:4
maxmemory:4
evicted_keys:1
```

설명:

> 각 엔트리는 키 1바이트와 값 1바이트로 총 2바이트입니다. `GET a`가 성공해 a가 MRU가 되고 b가 LRU가 됩니다. c를 저장하면 6바이트가 되어 제한 4바이트를 넘으므로 리스트 뒤쪽 b를 제거합니다. 이때 `used_memory`를 2 줄이고 메모리 정책에 의한 삭제이므로 `evicted_keys`를 1 증가시킵니다.

### 1-3. 메모리 정보와 UTF-8 공식

보여줄 코드:

- `mini_redis.py`: `_entry_size`, `info_memory`
- `test_mini_redis.py`: `test_utf8_memory_formula_and_overwrite`, `test_info_memory_has_required_fields`

설명:

> 공식은 `Σ(len(utf8(key)) + len(utf8(value)))`입니다. `가`와 `나`는 UTF-8에서 각각 3바이트이므로 `SET 가 나`의 사용량은 6바이트입니다. 값을 덮어쓸 때 이전 엔트리 크기를 먼저 빼고 새 엔트리 크기를 더해 중복 계산을 방지합니다. 노드나 포인터 등은 채점 공식에 따라 제외합니다.

### 1-4. TTL과 lazy deletion

보여줄 코드:

- `min_heap.py`: `peek`, `push`, `pop`, heapify 메서드
- `mini_redis.py`: `expire`, `ttl`, `_purge_expired`, `_expire_if_needed`
- `test_mini_redis.py`: `TTLTests`

CLI 시연:

```text
SET temp value
TTL temp
EXPIRE temp 3
TTL temp
# 3초 후
GET temp
TTL temp
```

예상 결과:

```text
TTL 설정 전 → (integer) -1
만료 후 GET → (nil)
만료 후 TTL → (integer) -2
```

설명:

> 힙에는 `(expire_at, key)`를 넣어 가장 이른 만료가 루트에 오게 합니다. TTL을 재설정하면 옛 힙 항목을 중간에서 억지로 삭제하지 않고 `ttl_map`에 최신 만료만 저장합니다. 옛 항목이 루트에 왔을 때 최신 값과 다르면 무시하는 것이 lazy deletion입니다.

### 1-5. 에러 처리

보여줄 코드:

- `mini_redis.py`: `execute`, `_wrong_number`, `config_set_maxmemory`, `set`
- `cli.py`: `shlex.split` 예외 처리
- `test_mini_redis.py`: `ErrorHandlingTests`, OOM 테스트

CLI 시연:

```text
HELLO
GET
CONFIG SET maxmemory abc
CONFIG SET maxmemory 3
SET long value
```

설명:

> 명령을 대문자로 정규화한 뒤 명령별 인자 수를 직접 검사합니다. 정수 변환은 `try/except`로 처리하고 음수도 같은 표준 정수 오류를 반환합니다. 새 단일 엔트리가 제한보다 크면 기존 데이터를 변경하기 전에 OOM을 반환합니다.

## 항목 2: 기본 자료구조 설명

### 2-1. 이중 연결 리스트가 O(1)인 이유

보여줄 코드:

- `linked_list.py`: `Node`, `remove_node`, `move_to_front`
- `test_linked_list.py`

답변:

> 노드는 `prev`, `next`, `data`를 가집니다. 삭제할 노드의 참조를 이미 알고 있다면 `node.prev.next`와 `node.next.prev`만 연결하면 되므로 전체 리스트를 순회하지 않습니다. 맨 앞 이동도 현재 앞뒤 연결을 끊고 `head` 앞에 연결하는 일정한 수의 포인터 변경만 수행하므로 `O(1)`입니다. 단, 노드를 먼저 검색해야 한다면 검색은 `O(n)`이므로 LRU에서는 별도 해시맵으로 노드 참조를 찾습니다.

### 2-2. 직접 만든 해시 함수

보여줄 코드:

- `hash_map.py`: `_hash`, `_bucket_index`

답변:

> 입력은 문자열 키입니다. 각 문자를 `ord`로 정수화하고 이전 결과에 31을 곱한 뒤 현재 문자 값을 더합니다. 이렇게 하면 문자 순서가 계산에 반영됩니다. 최종 정수 해시값을 현재 버킷 수로 나눈 나머지가 인덱스입니다. 같은 키는 같은 capacity에서 항상 같은 인덱스를 얻습니다. 키 길이를 k라고 하면 해시 계산은 `O(k)`이며, 전체 저장 항목 수 n에 대한 평균 조회를 `O(1)`이라고 표현합니다.

예:

```text
hash_value = hash_value * 31 + ord(char)
index = hash_value % capacity
```

### 2-3. 체이닝 충돌 해결

보여줄 코드:

- `hash_map.py`: `HashEntry`, `put`, `get`, `remove`
- `test_hash_map.py`: `test_collision_uses_bucket_chain`

답변:

> 서로 다른 키가 같은 버킷 인덱스를 받을 수 있는데 이를 충돌이라고 합니다. 각 버킷에 직접 만든 이중 연결 리스트를 두고, 같은 인덱스의 `HashEntry`를 연결하여 모두 보관합니다. 조회 시 먼저 버킷까지 바로 이동한 뒤 그 체인에서 실제 키를 비교합니다. 평균적으로 체인이 짧으면 `O(1)`이지만 모든 키가 한 버킷에 몰리는 최악의 경우는 `O(n)`입니다.

시연용 충돌:

```text
capacity = 8일 때 ord('a') % 8 == ord('i') % 8
```

### 2-4. 로드 팩터와 버킷 2배 확장

보여줄 코드:

- `hash_map.py`: `put`, `_resize`
- `test_hash_map.py`: `test_load_factor_over_point_75_doubles_capacity_and_rehashes`

답변:

> 로드 팩터는 `count / capacity`입니다. 새 키를 추가한 뒤 이 값이 0.75를 초과하면 기존 버킷 배열을 보관하고 capacity를 2배로 늘려 새 빈 배열을 만듭니다. 버킷 수가 바뀌면 `hash % capacity` 결과도 달라질 수 있으므로 기존 모든 항목을 새 인덱스로 다시 넣습니다. 이 과정이 리해싱이며 한 번의 리사이즈는 `O(n)`입니다. 정확히 0.75일 때가 아니라 초과할 때 확장합니다.

## 항목 3: 자료구조 조합 설명

### 3-1. LRU에서 해시맵과 이중 연결 리스트가 모두 필요한 이유

보여줄 코드:

- `mini_redis.py`: 생성자의 `lru_list`, `lru_nodes`, `_touch`
- `linked_list.py`: `move_to_front`

답변:

> 연결 리스트는 앞을 MRU, 뒤를 LRU로 유지하고 특정 노드를 `O(1)`에 이동·삭제합니다. 그러나 리스트만 사용하면 키의 노드를 찾는 데 `O(n)`이 걸립니다. `lru_nodes` 해시맵이 `key → Node`를 평균 `O(1)`에 찾아주고, 리스트가 그 노드를 `O(1)`에 맨 앞으로 옮깁니다. 그래서 평균적으로 조회와 갱신 모두 `O(1)`입니다.

### 3-2. TTL에 최소 힙을 사용한 이유

보여줄 코드:

- `min_heap.py` 전체
- `mini_redis.py`: `_purge_expired`

답변:

> 만료 관리에서 가장 필요한 것은 “지금 가장 빨리 만료될 항목”입니다. 일반 배열은 이를 찾을 때마다 전체를 보면 `O(n)`이지만, 최소 힙은 가장 작은 만료 시각을 루트에 유지하므로 `peek`가 `O(1)`입니다. 삽입과 루트 삭제도 `O(log n)`으로 처리합니다.

### 3-3. 메모리 초과 eviction 전체 흐름

보여줄 코드:

- `mini_redis.py`: `set`, `_evict_if_needed`, `_delete_key`, `_entry_size`

단계별 답변:

1. `SET` 전 만료된 키를 정리합니다.
2. 단일 새 엔트리의 UTF-8 바이트가 제한보다 크면 상태를 바꾸지 않고 OOM을 반환합니다.
3. 덮어쓰기라면 이전 엔트리 크기를 뺍니다.
4. 새 값을 저장하고 새 엔트리 크기를 더합니다.
5. 키를 MRU 위치로 옮깁니다.
6. `used_memory > maxmemory`인 동안 `lru_list.tail`의 키를 삭제합니다.
7. 삭제할 때 해당 엔트리 크기를 차감하고 데이터·LRU·TTL 구조에서 모두 제거합니다.
8. 메모리 정책으로 제거한 경우에만 `evicted_keys`를 증가시킵니다.

### 3-4. GET 전체 흐름

보여줄 코드:

- `mini_redis.py`: `get`, `_expire_if_needed`, `_purge_expired`, `_touch`

답변:

> 먼저 힙을 통해 만료된 키를 삭제합니다. 대상 키가 만료됐거나 존재하지 않으면 `(nil)`을 반환하고 LRU는 갱신하지 않습니다. 살아 있는 값을 찾았을 때만 노드를 리스트 맨 앞으로 옮기고 따옴표로 감싼 값을 반환합니다.

## 항목 4: 확장 질문 답변

### 4-1. LRU 대신 LFU라면?

답변:

> LRU는 “언제 마지막으로 사용했는가”를 리스트 순서로 관리하지만 LFU는 “몇 번 사용했는가”를 관리해야 합니다. 각 키의 빈도수를 저장하고 GET/SET 성공 때 증가시켜야 합니다. 단순 구현은 `(frequency, time, key)` 최소 힙을 사용하여 가장 낮은 빈도를 제거할 수 있지만 갱신이 `O(log n)`입니다. 평균 `O(1)`을 목표로 한다면 직접 만든 해시맵으로 `key → 빈도 노드`, 또 다른 해시맵으로 `frequency → 이중 연결 리스트`를 관리하고 `min_frequency`를 기억합니다. 같은 빈도 안에서는 리스트 순서로 오래된 키를 선택합니다.

보여줄 현재 코드:

- `mini_redis.py`의 `lru_list`, `lru_nodes`, `_touch`를 가리키며 LFU에서는 이 부분이 빈도 버킷 구조로 교체된다고 설명합니다.

### 4-2. 데이터가 10만 건이면 어디가 병목인가?

답변:

- `KEYS`는 모든 키를 모으고 출력하므로 `O(n)`이며 결과 메모리도 큽니다. 실제 운영형 설계라면 커서 기반의 점진적 조회를 사용합니다.
- 해시맵 리사이즈는 순간적으로 전 항목을 리해싱해 `O(n)` 지연이 생깁니다. 점진적 리해싱으로 여러 명령에 작업을 분산할 수 있습니다.
- 해시 충돌이 많으면 체인이 길어져 조회가 느려집니다. 더 좋은 해시 함수, 충분한 버킷, 충돌 분포 검사가 필요합니다.
- TTL 재설정이 많으면 lazy deletion의 오래된 힙 항목이 쌓일 수 있습니다. 주기적 힙 재구성 또는 키의 힙 위치를 추적하는 indexed heap을 고려할 수 있습니다.
- 키가 매우 길면 해시 계산 자체가 키 길이 `k`에 비례합니다.
- 한 프로세스의 메모리 한계를 넘는 규모에서는 샤딩·클러스터를 고려할 수 있지만 이번 CLI 과제 범위는 아닙니다.

보여줄 코드:

- `hash_map.py`: `keys`, `_resize`, `_hash`
- `mini_redis.py`: `_purge_expired`

### 4-3. 자료구조 오버헤드를 used_memory에 포함한다면?

답변:

> 현재 공식은 키와 값의 UTF-8 바이트만 세므로 언어와 실행 환경이 달라도 결과를 비교하기 쉽습니다. 실제 오버헤드를 포함하면 Node 객체, `prev`·`next` 참조, HashEntry, 버킷 배열, 힙 배열의 빈 capacity, Python 객체 헤더까지 계산해야 합니다. 이 크기는 Python 버전, 32/64비트, 구현체에 따라 달라질 수 있어 같은 데이터도 결과가 달라집니다.

공정한 비교 방법:

1. 과제처럼 논리적 메모리 공식을 고정합니다.
2. 또는 노드·포인터·버킷별 고정 가상 비용을 명시해 모두 같은 모델을 사용합니다.
3. 실제 측정이 목적이면 동일 Python 버전·운영체제·아키텍처에서 같은 측정 도구와 깊은 크기 계산 규칙을 사용합니다.
4. 채점 기대값도 새 공식에 맞게 다시 계산해야 하며, 기존 `maxmemory` 테스트값을 그대로 사용하면 안 됩니다.

보여줄 현재 코드:

- `mini_redis.py`: `_entry_size`
- README의 메모리 규칙

## 마지막 더블체크

평가 직전에 다음을 확인합니다.

- [ ] `python3 -m unittest -v` 전체 통과
- [ ] `dict`, `set`, `collections`, `heapq` 미사용
- [ ] 기본 명령 6개 직접 시연
- [ ] maxmemory와 LRU 제거 시연
- [ ] INFO memory의 3개 항목 확인
- [ ] TTL의 `-1`, `-2`, 만료 시연
- [ ] unknown command, wrong arguments, integer error, OOM 시연
- [ ] 해시 충돌과 리사이즈 테스트 위치 설명
- [ ] GET 흐름을 TTL → 조회 → LRU 순서로 설명
- [ ] LFU·10만 건·오버헤드 확장 질문 답변 연습
